{% extends "layout.html.twig" %}

{% block title %}Connexion | StagePro{% endblock %}

{% block content %}
<section style="max-width: 500px; margin: 3rem auto; background: var(--surface, #ffffff); padding: 2rem; border: 1px solid var(--border, #e5e7eb); border-radius: 8px; box-shadow: 0 4px 6px -1px rgba(0, 0, 0, 0.1);">
    <h1 style="margin-top: 0; color: var(--text-main, #111827);">Connexion</h1>
    <p style="color: var(--text-muted, #6b7280); margin-bottom: 1.5rem;">Connectez-vous à votre espace StagePro.</p>

    {% if erreur %}
        <div style="background: #ffdede; color: #a10000; padding: 12px; border-radius: 6px; margin-bottom: 1.5rem; border: 1px solid #ffbaba; font-size: 0.9rem; text-align: center;">
            <strong>Erreur :</strong> {{ erreur }}
        </div>
    {% endif %}

    <form action="index.php?page=login" method="post">
        <div class="form-group" style="margin-bottom: 1.5rem;">
            <label for="email" style="display: block; margin-bottom: 0.5rem; font-weight: 600; font-size: 0.9rem;">Email</label>
            <input type="email" id="email" name="email" placeholder="votre@email.fr" required 
                   style="width: 100%; padding: 0.75rem; border-radius: 6px; border: 1px solid var(--border, #d1d5db); background: var(--background, #ffffff); color: var(--text, #111827); box-sizing: border-box; font-size: 1rem;">
        </div>

        <div class="form-group" style="margin-bottom: 2rem;">
            <label for="password" style="display: block; margin-bottom: 0.5rem; font-weight: 600; font-size: 0.9rem;">Mot de passe</label>
            <input type="password" id="password" name="password" placeholder="••••••••" required 
                   style="width: 100%; padding: 0.75rem; border-radius: 6px; border: 1px solid var(--border, #d1d5db); background: var(--background, #ffffff); color: var(--text, #111827); box-sizing: border-box; font-size: 1rem;">
        </div>

        <button type="submit" style="width: 100%; background: #2563eb; color: white; padding: 1rem; border: none; border-radius: 6px; cursor: pointer; font-weight: 700; font-size: 1rem;">
            Se connecter
        </button>
    </form>
    
    <div style="margin-top: 1.5rem; text-align: center; font-size: 0.85rem; color: var(--text-muted, #6b7280);">
        <p>Problème d'accès ? Contactez un pilote ou l'administrateur.</p>
    </div>
</section>
{% endblock %}